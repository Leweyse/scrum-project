import { Navbar, Footer, ProfileSection } from '../../components';

export default function ProfilePage (props) {
    return (
        <>
            <Navbar />
            <ProfileSection />
            <Footer />
        </>
    )
}